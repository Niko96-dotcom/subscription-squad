# Pull request

## Summary

- What changes, and why:

## Checks

- [ ] `make ci` passes locally (paste the result, not the whole log)
- [ ] Examples and docs match the actual CLI contracts in `scripts/worker.py`
      and `scripts/collect.py` (flags, defaults, exit codes)
- [ ] No secrets, tokens, private logs, or customer data included

## Scope

- Files changed:
- Linked issue (if any):

## Notes for reviewers

- Anything the reviewer should verify by hand (diff inspection, focused
  test commands, model inventory state at the time):
