interface:
  display_name: "Subscription Squad"
  short_description: "External workers plus review, led by Codex"
  default_prompt: "Use $subscription-squad for this task, where Codex coordinates and accepts while substantive work dispatches at least one bounded external worker."
